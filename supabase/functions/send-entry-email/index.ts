const RESEND_API_KEY=Deno.env.get("RESEND_API_KEY");const RESEND_FROM_EMAIL=Deno.env.get("RESEND_FROM_EMAIL")||"Bagger EZ80 <onboarding@resend.dev>";const EMAIL_TO="Bagger.Wartbergnussbach@gmail.com";const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};function esc(v){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders});if(!RESEND_API_KEY)return new Response(JSON.stringify({error:"RESEND_API_KEY fehlt."}),{status:500,headers:{...corsHeaders,"Content-Type":"application/json"}});try{const{entry,submittedBy}=await req.json();const usage=entry.usage_type==="ueberbetrieblich"?"überbetrieblich":"innerbetrieblich";const subject=`Neuer Bagger EZ80 Eintrag: ${entry.member_name||submittedBy||"Unbekannt"} - ${entry.date||""}`;const text=["Neuer Eintrag für Bagger EZ80","",`Erfasst von: ${submittedBy||"Unbekannt"}`,`Datum: ${entry.date||""}`,`Mitglied / Fahrer: ${entry.member_name||""}`,`Betriebsstunden: ${entry.hours??""}`,`Getankte Dieselmenge: ${entry.diesel_liters??0} Liter`,`Einsatzart: ${usage}`,`Bemerkung / Schäden: ${entry.note||"-"}`,`Erfasst am: ${entry.created_at||""}`].join("\n");const html=`<h2>Neuer Eintrag für Bagger EZ80</h2><table cellpadding="8" cellspacing="0" border="1" style="border-collapse:collapse;font-family:Arial,sans-serif;font-size:14px;"><tr><td><strong>Erfasst von</strong></td><td>${esc(submittedBy||"Unbekannt")}</td></tr><tr><td><strong>Datum</strong></td><td>${esc(entry.date)}</td></tr><tr><td><strong>Mitglied / Fahrer</strong></td><td>${esc(entry.member_name)}</td></tr><tr><td><strong>Betriebsstunden</strong></td><td>${esc(entry.hours)}</td></tr><tr><td><strong>Getankte Dieselmenge</strong></td><td>${esc(entry.diesel_liters??0)} Liter</td></tr><tr><td><strong>Einsatzart</strong></td><td>${esc(usage)}</td></tr><tr><td><strong>Bemerkung / Schäden</strong></td><td>${esc(entry.note||"-")}</td></tr><tr><td><strong>Erfasst am</strong></td><td>${esc(entry.created_at||"")}</td></tr></table>`;const r=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${RESEND_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({from:RESEND_FROM_EMAIL,to:[EMAIL_TO],subject,text,html})});const j=await r.json();if(!r.ok)return new Response(JSON.stringify({error:j}),{status:r.status,headers:{...corsHeaders,"Content-Type":"application/json"}});return new Response(JSON.stringify({ok:true}),{headers:{...corsHeaders,"Content-Type":"application/json"}})}catch(error){return new Response(JSON.stringify({error:String(error)}),{status:500,headers:{...corsHeaders,"Content-Type":"application/json"}})}});